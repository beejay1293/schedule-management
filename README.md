# Schedule Management

## Run locally
1. Generate proto code:
   make gen-proto
2. Start backend gRPC server:
   make run-backend
3. Start HTTP bridge:
   make run-http
4. Start frontend:
   cd frontend && npm run dev
